{
  "name": "magicshow",
  "version": "1.0.0",
  "description": "Gasp Machine magic club website + a tiny backend that proxies the YouTube playlist so the API key stays server-side.",
  "type": "commonjs",
  "main": "src/server/server.js",
  "scripts": {
    "start": "node src/server/server.js",
    "dev": "node --watch src/server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
